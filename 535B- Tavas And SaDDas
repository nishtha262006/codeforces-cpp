#include <iostream>
#include <string>
using namespace std;

int main() {
    string s;
    cin >> s;

    int ans = 0;

    for (char ch : s) {
        ans *= 2;

        if (ch == '7')
            ans++;
    }

    ans += (1 << s.size()) - 2;

    cout << ans + 1;

    return 0;
}
